# CLAUDE.md

**See [AI_RULES.md](./AI_RULES.md) for development standards and conventions.**

This project uses **bun** as the package manager and script runner. Use `bun run <script>` (e.g. `bun run dev`, `bun run test`, `bun run check`, `bun run build`), not npm.
